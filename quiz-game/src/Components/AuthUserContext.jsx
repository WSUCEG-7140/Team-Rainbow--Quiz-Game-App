import React from "react";

// @ref R37_0 

//This Component is part of the @ref Model within the overall @ref ModelViewController model.
//This Component implements the methods related to authentication of the user the user.

/**
 * AuthUserContext functionality using React's Context API
 * 
 */
const AuthUserContext = React.createContext(null); //using React's Context API

export default AuthUserContext;